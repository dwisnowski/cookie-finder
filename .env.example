# Copy to .env and fill in secrets (.env is gitignored).
# Used by: make on-the-pi-init-cloudflare-tunnel

# Cloudflare Zero Trust → Networks → Tunnels → Create → copy install token
# (the long token passed to: sudo cloudflared service install <token>)
CLOUDFLARE_TUNNEL_TOKEN=replace-with-cloudflare-tunnel-token
